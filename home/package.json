{
  "name": "peru-home",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "DEPLOY_TARGET=cloudflare vite",
    "dev:cloudflare": "DEPLOY_TARGET=cloudflare vite",
    "dev:vercel": "DEPLOY_TARGET=vercel vite",
    "dev:local": "DEPLOY_TARGET=local vite",
    "build": "DEPLOY_TARGET=cloudflare vite build",
    "build:cloudflare": "DEPLOY_TARGET=cloudflare vite build",
    "build:vercel": "DEPLOY_TARGET=vercel vite build",
    "build:local": "DEPLOY_TARGET=local vite build",
    "preview": "npm run build && wrangler dev --local-protocol https",
    "preview-qa": "CLOUDFLARE_ENV=qa npm run build && wrangler dev --local-protocol https",
    "deploy": "npm run build && wrangler deploy",
    "deploy-qa": "CLOUDFLARE_ENV=qa npm run build && wrangler deploy",
    "proxy": "microfrontends proxy --local-apps peru-home"
  },
  "dependencies": {
    "@tolgee/react": "^6.2.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.11.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.15.2",
    "@vercel/microfrontends": "^2.3.2",
    "@vitejs/plugin-legacy": "^6.1.1",
    "@vitejs/plugin-react": "^4.3.4",
    "terser": "^5.46.0",
    "vite": "^6.2.1",
    "wrangler": "^4.70.0"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Firefox versions",
    "last 2 Safari versions",
    "Chrome >= 70",
    "Firefox >= 68",
    "Safari >= 12",
    "iOS >= 12",
    "Edge >= 79",
    "not dead"
  ]
}
